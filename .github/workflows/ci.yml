name: CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'ingestion/**'
      - 'docker-compose.yaml'

jobs:
  deploy:
    runs-on: datahub-runner
    
    steps:
      - name: Check out the repository code
        uses: actions/checkout@v4

      - name: Deploy DataHub to EC2
        run: |
          echo "🚀 Deploying DataHub to EC2..."
          cd /home/ubuntu/datahub

          echo "📦 Pulling latest code..."
          git pull origin main

          echo "🏃 Running deployment script..."
          bash -eu .github/workflows/ec2-deploy.sh
